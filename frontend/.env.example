# Copy to `.env.local` and fill in. Picked up by Vite at build time and
# baked into the SPA bundle as `import.meta.env.VITE_*` constants.
#
# In production (Railway) these are passed as Docker build args from the
# service Variables — see Dockerfile.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
